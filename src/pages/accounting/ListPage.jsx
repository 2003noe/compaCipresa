import Card from '../../components/ui/Card';import Table from '../../components/ui/Table';import Badge from '../../components/ui/Badge';import Button from '../../components/ui/Button';import {Plus,Download,Filter} from 'lucide-react';import {useNavigate} from 'react-router-dom';
export default function ListPage({title,subtitle,columns,rows,primary='Nouvel élément',primaryPath,headerOnly=false}){const navigate=useNavigate();const go=()=>primaryPath?navigate(primaryPath):null;return <div className="page-content"><div className="page-header"><div><h1 className="page-title">{title}</h1><p className="page-subtitle">{subtitle}</p></div><div className="flex gap-2"><Button variant="secondary" icon={Download}>Exporter</Button>{primary&&<Button icon={Plus} onClick={go}>{primary}</Button>}</div></div>{!headerOnly&&<Card><div className="list-toolbar"><div className="toolbar-search">Rechercher...</div><Button variant="secondary" size="sm" icon={Filter}>Filtres</Button></div><Table columns={columns} rows={rows||[]} renderCell={(r,k)=>['status','statut'].includes(k)?<Badge>{r[k]}</Badge>:r[k]}/></Card>}</div>}
