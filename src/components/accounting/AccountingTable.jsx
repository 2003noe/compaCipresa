import Card from '../ui/Card';import Table from '../ui/Table';import Badge from '../ui/Badge';
export default function AccountingTable({title='Écritures récentes',rows}){const columns=[{key:'date',label:'Date'},{key:'ref',label:'Référence'},{key:'description',label:'Libellé'},{key:'debit',label:'Débit',align:'right'},{key:'credit',label:'Crédit',align:'right'},{key:'status',label:'Statut'}];return <Card className="table-card"><div className="card-heading"><div><h2>{title}</h2><p>Dernières opérations comptabilisées</p></div><button className="text-button">Voir tout →</button></div><Table columns={columns} rows={rows} renderCell={(r,k)=>k==='status'?<Badge>{r[k]}</Badge>:r[k]}/></Card>}
