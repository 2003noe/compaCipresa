import {Search,CalendarDays,Bell,Plus} from 'lucide-react';import Button from '../ui/Button';import {useLocation,useNavigate} from 'react-router-dom';
export default function TopBar(){const location=useLocation();const navigate=useNavigate();const goNew=()=>{if(location.pathname==='/journal')navigate('/nouvelle-ecriture');else if(location.pathname==='/plan-comptable')navigate('/nouveau-compte');else navigate('/nouvelle-ecriture')};return <header className="topbar"><div className="topbar-left"><div className="topbar-search"><Search size={16}/><input placeholder="Rechercher un compte, écriture..."/></div><button className="period-selector"><CalendarDays size={14}/><b>Exercice 2025-2026</b></button></div><div className="topbar-right"><button className="icon-button"><Bell size={16}/><span className="notification-dot"/></button><Button size="sm" icon={Plus} onClick={goNew}>Nouvelle écriture</Button></div></header>}
